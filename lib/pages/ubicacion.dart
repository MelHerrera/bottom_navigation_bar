import 'package:flutter/cupertino.dart';

class ubicacion extends StatelessWidget {
  const ubicacion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("pantalla de ubicaciones");
  }
}
